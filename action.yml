name: 'Releaser'
description: 'Create a release'
inputs:
  mode: # minor/major/pre
    required: true
  prerelease:  # true/false
    required: true
runs:
  using: "composite"
  steps:
    - shell: bash
      run: |
        python -u "${{ github.action_path }}/src/test.py" $GITHUB_ENV
    - shell: bash
      run: |
        echo $TEST
    # - name: handle inputs
    #   shell: bash
    #   run: |
    #     python -u "${{ github.action_path }}/src/handle_inputs.py"
    #   env:
    #     WORK_PATH: ${{ github.workspace }}
    #     PRERELEASE: ${{ inputs.prerelease }}
    #     MODE: ${{ inputs.mode }}
    # - name: get last release informaton
    #   shell: bash
    #   run: |
    #     python -u '${{ github.action_path }}/src/get_last_release_information.py'
    #   env:
    #     GITHUB_TOKEN: ${{ github.token }}
    #     WORK_PATH: ${{ github.workspace }}
    # - name: generate new release information
    #   shell: bash
    #   run: |
    #     python -u '${{ github.action_path }}/src/generate_new_release_information.py'
    #   env:
    #     PRERELEASE: ${{ inputs.prerelease }}
    #     WORK_PATH: ${{ github.workspace }}
    # - name: Create Release
    #   uses: softprops/action-gh-release@v2.0.6
    #   with:
    #     draft: ${{ true }}
    #     prerelease: ${{ inputs.prerelease }}
